# Production build config — the backend colocated in the shared "been"
# Supabase project (see supabase/colocation/README.md). Both values are PUBLIC
# by design and safe to commit and ship in the client bundle: the publishable
# (anon) key can only call the potluck_* RPC functions, and every table is
# RLS deny-all in a private schema. Vercel project env vars, if set, take
# precedence over this file.
VITE_SUPABASE_URL=https://mjkimkaggayvovwscrqi.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Zy7PJGXPrwZZuoqs_OQIFQ_88PxELG7
